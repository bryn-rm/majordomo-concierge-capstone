"""
Simple deterministic math helpers.

Useful for testing that tool calls work end-to-end.
"""


def add_numbers(a: float, b: float) -> float:
    return a + b


def subtract_numbers(a: float, b: float) -> float:
    return a - b
